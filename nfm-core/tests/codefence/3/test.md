```rust
let x = String::new();
x.push_str("Hello, World!");
println!("{x}");
```

```markdown
# Test

Let's see if `nfm` can document itself in code fences!

Might be a tall order.

Here's some "meta" code fencing:

\`\`\`markdown
# Markdown

inside of markdown!
\`\`\`
```
